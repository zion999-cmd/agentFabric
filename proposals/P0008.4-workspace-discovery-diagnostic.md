# P0008.4 Workspace Discovery Diagnostic

**Date**: 2026-08-13
**Type**: A/B 诊断实验 — 未改 AGENTS.md，未改 Contract，未改生产代码

---

## 1. Diagnostic Matrix

| Test | cwd | read_file | search_files | web_search | write_file | terminal |
|------|-----|-----------|--------------|-----------|-----------|----------|
| A (cwd 验证) | Fabric WS | — | — | — | — | — |
| B (纯 lookup) | Fabric WS | 0 | 0 | **2** | 0 | 0 |
| C (explicit compile) | Fabric WS | **1** | 0 | **0** | 0 | **3** |
| D (historical control) | WorldExplorationTask | 0 | 0 | 0 | 0 | 0 |

**Test A**：cwd root = `AGENTS.md, README.md, capability, knowledge, knowledge-sources, world`；raw = `marketing-case.md, platform-promotion.md`。**cwd 正确。**

**Test B**（"京东有哪些推广方式？"）：web_search=2，无本地读。→ lookup 语义触发外部搜索。

**Test C**（"把已有的推广方式资料整理成共享知识"）：read_file=1 + terminal=3，**web_search=0**。→ compile 语义触发 filesystem 读。

**Test D**（"竞品对比指标有哪些？" + blank profile）：**零工具**。→ blank profile 无 JD soul/skills，直接凭训练数据回答，未扫描 WorldExplorationTask 文件。

---

## 2. 回答四个问题

### Q1: Fabric Workspace cwd 是否真的正确？

**是。** Test A 确认 cwd root 精确包含 AGENTS.md + world/ + knowledge/ + knowledge-sources/，raw 源存在。session.create 传入的 cwd 就是 Hermes 实际看到的 cwd。

### Q2: Hermes filesystem-native discovery 是否仍然成立？

**成立。** Test C 证明：当任务是"整理成共享知识"（compile 语义），Hermes 使用 read_file + terminal（filesystem 探索），**web_search=0**。filesystem discovery 对 compile 类任务完全正常工作。

### Q3: 上次失败主要来自 cwd、workspace structure、task wording，还是 instruction priority？

**task wording（"最新"），不是 cwd / structure / instruction。**

证据：
- Test B（"有哪些" → lookup）→ web_search，因为"有哪些"是 question 语义，Agent 合理判断需要外部/最新信息
- Test C（"整理" → compile）→ filesystem 读，因为"整理"是 compile 语义，Agent 合理判断读本地
- 上次 Acceptance 的 "最新" → research intent → web_search，是 **source selection 的正常推理**

### Q4: 是否真的有证据需要修改 AGENTS.md？

**没有。** AGENTS.md 对 compile 类任务（Test C）已经正确引导 Hermes 走 filesystem。上次的 web_search × 9 是"最新"触发的 research intent，不是 AGENTS.md 指引不足。

---

## 3. 核心区分（用户判断正确）

```
filesystem discovery   = Agent 知不知道本地有知识（Test C 证明：知道）
source selection       = Agent 判断这次任务该用本地还是外部（Test B/C 证明：按任务语义判断）
```

这两个是不同的问题。Test C 证明 discovery 成立；Test B/C 证明 source selection 按任务语义（lookup→web / compile→local）正确工作。

**"最新" 这个词是上一次 web_search 的根因，不是 AGENTS.md 的问题。**

---

## 4. 额外发现（Test D 的启示）

Test D（blank profile + WorldExplorationTask cwd + "竞品对比指标"）→ **零工具**。

这说明历史人工测试的成功**主要来自 `jdsz-project` profile 的 JD soul/skills**（Hermes 被 profile 告知了 JD 知识），而不是 filesystem 扫描 WorldExplorationTask 目录。

这进一步说明：
- profile（SOUL/skills）让 Hermes "知道 JD"，从而能正确回答"竞品对比指标"
- blank profile + 纯 cwd 文件，不足以让 Hermes 主动发现并使用这些文件（除非任务语义明确指向 compile）

---

## 5. 结论

1. cwd 正确，filesystem discovery 成立（compile 任务）
2. 上次失败 = "最新" 触发 research intent，是合理的 source selection
3. **无证据需要修改 AGENTS.md**
4. 真正需要区分的是：discovery（已成立）vs source selection（按任务语义，正常）

---

*诊断完成。未改 AGENTS.md / Contract / 生产代码。*
