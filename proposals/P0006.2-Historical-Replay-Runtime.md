P0006.2 — Historical Replay Runtime（小补丁）
🎯 目标

新增一个 Replay Runner。

它只是负责：

日期循环
    ↓
调用 Runtime Kernel

除此之外，不增加任何业务逻辑。

设计原则（必须遵守）

Replay 不是 Import。

Replay 不是 Mock。

Replay 不是特殊模式。

Replay 唯一做的事情：

Acquire

从

Live Connector

换成

Historical Connector

后面的：

Signal

Evidence

Ranking

Memory

Decision

全部保持不变。

新增
1）Replay Runner

新增：

apps/ecommerce/runtime/replay/
    replay-runner.ts

负责：

for each day

↓

kernel.execute()

例如：

await replayRunner.run({
    shopId,
    from: "2026-01-01",
    to: "2026-07-01"
})
2）Replay Connector

新增：

connectors/jd/history/

职责只有一个：

指定日期

↓

获取当天数据

↓

返回 AcquireResult

返回格式必须与 Live Connector 一致。

Kernel 不需要知道数据来自哪里。

3）Workspace

Runtime 页面新增：

Replay

Shop

Start

End

[ Run Replay ]

点击：

POST

/api/runtime/replay
4）HTTP

新增：

POST

/api/runtime/replay

参数：

{
  "shopId":"jd_shop_001",
  "from":"2026-01-01",
  "to":"2026-07-01"
}

返回：

{
  "days":183,
  "completed":183,
  "signals":4560,
  "executions":183
}
Kernel

Kernel 不要增加新的 Runtime。

Replay Runner 内部只是：

for day

↓

kernel.execute()

Kernel API 不变。

不允许

Replay 内禁止：

ReplaySignal

ReplayEvidence

ReplayRanking

ReplayMemory

这些都不能出现。

禁止：

if (replay)

禁止：

ReplayPipeline
验收

Replay：

2026-01-01

↓

Runtime

↓

Execution

↓

Signal

↓

Evidence

↓

Ranking

连续：

183 天

最终：

Execution History：

183 runs

Signal：

4xxx+

Workspace：

Trend：

真正出现半年曲线

Product：

真正出现生命周期

Memory：

真正开始积累

这是一个很小的补丁，预计改动主要集中在：

新增 replay-runner.ts
新增历史数据 Connector
新增一个 HTTP Route
Workspace 增加一个 Replay 面板

Kernel、Signal、Evidence、Ranking、Memory 都不需要修改。 这符合我们现在的原则：尽量不动已经稳定收敛的核心。