P0005.5 — Runtime Convergence Layer（统一运行时收敛层）
🎯 核心目标（一句话）

让系统从 “Discovery → Capability → Connector → Execution（多宇宙）”
收敛为：

Discovery → Capability → Blueprint → Runtime Kernel → Signals/Evidence
一、当前系统真实状态（基于审计 + 代码）
❗1. 架构现状本质（不是你文档写的那种）
现在是 3 套运行体系并存：
(1) CLI Legacy Path
processDay()
  ├── evidence.store
  ├── normalizeSignal(JD_SPEC)
  └── SignalFacade.store

(2) Blueprint Path（P0005.4）
executePlan()
  └── acquireFn()

(3) Binding Layer
planner.ts / executor.ts
  └── ❌ 未进入 CLI 主链路
❗2. 关键断裂点（深度审计 + 代码验证）
❌ A. Runtime split（最严重）

CLI 根本没用：

executePlan()
createPlatformExecutor()

👉 说明：

P0005.4 是“存在”，不是“运行时生效”

❌ B. Signal pipeline 双轨制
系统	是否真实
processDay	✔ 主生产路径
executor pipeline	❌ 未接入
❌ C. Blueprint 只影响 acquisition
acquireJdData(blueprint?) // optional

但：

❌ 不影响 signal
❌ 不影响 normalizer
❌ 不影响 processDay
❌ D. normalizer-plan.json 完全未进入 runtime
二、P0005.5 设计目标（收敛三层系统）
🎯 P0005.5 = Runtime Convergence Layer
🔥 核心使命：

让“蓝图驱动”成为唯一 runtime truth

三、最终架构（P0005.5 After）
            ┌────────────────────────────┐
            │   Discovery Layer (P0005.2) │
            └────────────┬───────────────┘
                         ↓
            ┌────────────────────────────┐
            │ Capability Generator P0005.3│
            └────────────┬───────────────┘
                         ↓
            ┌────────────────────────────┐
            │ Blueprint (P0005.3 output)  │
            └────────────┬───────────────┘
                         ↓
        ┌────────────────────────────────────┐
        │   P0005.5 Runtime Convergence      │
        │  (NEW CORE LAYER)                  │
        ├────────────────────────────────────┤
        │ - Plan Router                      │
        │ - Execution Kernel                │
        │ - Signal Generator                │
        │ - Evidence Orchestrator           │
        │ - Normalizer Resolver             │
        └────────────┬──────────────────────┘
                     ↓
         Signals / Evidence / DB
四、P0005.5 核心模块设计
📦 1. runtime/kernel/
① runtime-router.ts（替换 CLI dispatch）
作用：

👉 决定执行路径（不是 CLI switch）

routeExecution(blueprint, context)

输出：

ExecutionPlan

✔ 替代：

cli.ts processDay switch-case
hardcoded signal_type logic
② runtime-executor.ts（升级版 executor）

基于现有 executor.ts，但增强：

新能力：
✔ signal generation hook
✔ evidence emission hook
✔ normalizer injection
✔ context-aware execution
③ runtime-signal-engine.ts（新增）
替代：
SignalFacade.store(normalizeSignal())
改为：
Execution → Signal Engine → normalized output

✔ 支持：

blueprint-driven signal_type
dynamic metrics mapping
confidence scoring hook
④ runtime-evidence-orchestrator.ts（新增）

统一：

saveEvidence()

从：

processDay()
import-jd()
acquireJdData()

收敛为：

EvidenceOrchestrator.write()
⑤ runtime-normalizer-resolver.ts（关键修复）
解决审计问题：

❌ normalizer-plan.json 没有使用

新逻辑：

优先级：

1. INDICATOR_OVERRIDES
2. generated/normalizer-plan.json   ← P0005.3产物（现在要启用）
3. JD_SPEC fallback
五、CLI 重构（关键修复点）
❗当前问题：
cli.ts
  ├── processDay ❌ legacy runtime
  ├── acquireJdData ❌ bypass blueprint
  └── SignalFacade ❌ direct write
✔ P0005.5 改造：
CLI 变成：
CLI → RuntimeKernel → EVERYTHING
✳ 修改结构：
1. 删除逻辑（不删文件，只收敛）
processDay()
SignalFacade.store()
normalizeSignal()

👉 移入 runtime-signal-engine

2. CLI 新逻辑：
const kernel = createRuntimeKernel(blueprint)
await kernel.execute({
  shopId,
  date,
  mode: 'mock|live'
})
六、executor.ts 的升级（关键修复）
❗当前问题：

executor 只是：

acquire → parsed raw

❌ 没有 runtime semantics

✔ P0005.5 升级：

executor 变为：

acquire
→ parse
→ normalize
→ signal emit
→ evidence emit
七、必须修复的 5 个 CRITICAL GAP
🔴 GAP 1：CLI bypass blueprint
修复：

CLI 必须只调用：

RuntimeKernel
🔴 GAP 2：processDay 消失

👉 迁移到 runtime-signal-engine

🔴 GAP 3：normalizer-plan.json 未使用

👉 必须进入 runtime-normalizer-resolver

🔴 GAP 4：binding layer 未进入 production path

👉 planner/executor → kernel core

🔴 GAP 5：executor 和 processDay 双 runtime

👉 合并为 single execution pipeline

八、完善项（非阻塞但重要）
🟡 1. Router prompt hardcoding

→ 迁移到:

skill definitions / runtime prompts
🟡 2. Tmall stub

→ 接入 runtime kernel 后统一处理 adapter

🟡 3. coverage.ts 假 100%

→ runtime-driven coverage recalculation

🟡 4. CDP default APIs

→ blueprint-driven endpoint selection

九、P0005.5 最终交付物
📦 新增模块
apps/ecommerce/runtime/
  kernel/
    runtime-kernel.ts
    runtime-router.ts
    runtime-executor.ts
    runtime-signal-engine.ts
    runtime-evidence-orchestrator.ts
    runtime-normalizer-resolver.ts
🔧 修改模块
cli.ts → 变成 kernel entry
connectors/jd/ → 变 thin adapter
binding/ → upgrade to kernel dependency
executor.ts → become internal runtime primitive
十、最终系统形态（完成后）
ONE RUNTIME ONLY

CLI
 ↓
Runtime Kernel
 ↓
Blueprint
 ↓
Execution Plan
 ↓
Acquisition
 ↓
Signal Engine
 ↓
Evidence Store
 ↓
SQLite
十一、关键判断（非常重要）
❗P0005.4 vs P0005.5 本质区别：
版本	本质
P0005.4	capability 连接层
P0005.5	runtime 收敛层（系统统一入口）
十二、最终一句话总结

P0005.4 解决的是“数据怎么进来”
P0005.5 解决的是“系统怎么统一运行”